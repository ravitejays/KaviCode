"""Kavi's extension layers: skills, hooks, custom commands, and plugins.

All of these are auto-discovered from a Kavi-native ``.kavi/`` layout and a
Claude-Code-compatible ``.claude/`` layout, so an existing Claude Code setup
works unchanged. See :mod:`kavi.extensions.sources` for the search order.
"""

from __future__ import annotations
